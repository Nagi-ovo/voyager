{
  "$comment": "Published primitive contracts (plan D9). verbs/contracts.test.ts fails when a listed primitive disappears, a listed param changes type or becomes required, or a new param is required. Append here when a primitive ships; never edit an existing entry.",
  "primitives": {
    "codeCollapse": {
      "sinceEngine": "1.5.0",
      "params": {
        "code": { "type": "selector", "required": false },
        "thresholdLines": { "type": "number", "required": false }
      }
    },
    "tableCopy": {
      "sinceEngine": "1.5.0",
      "params": { "table": { "type": "selector", "required": false } }
    },
    "formulaCopy": {
      "sinceEngine": "1.3.0",
      "params": {}
    },
    "vimInput": {
      "sinceEngine": "1.4.0",
      "params": {
        "composer": {
          "type": "selector",
          "required": false
        }
      }
    },
    "turnNavigator": {
      "sinceEngine": "1.4.0",
      "params": {
        "turn": {
          "type": "selector",
          "required": false
        },
        "conversationIdPattern": {
          "type": "string",
          "required": false
        },
        "scrollContainer": {
          "type": "selector",
          "required": false
        },
        "yieldWhen": {
          "type": "selector",
          "required": false
        },
        "position": {
          "type": "string",
          "required": false
        }
      }
    }
  }
}
